# Source Code

This folder contains the source code modules for the Water Pump RUL project.

The source files implement the core algorithms and helper functions used in the assignment

The project is executed through `main.py` in the root directory, while the files in this folder provide the underlying implementation for the full pipeline.

The final methodology, results, and discussion are documented in `report.md`.
